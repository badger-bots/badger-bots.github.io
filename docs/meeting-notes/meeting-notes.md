---
title: Meeting Notes
layout: default
nav_order: 1
has_children: true
# Renders the nav entry as plain text rather than a link, so this section
# cannot be clicked into (see _includes/components/nav/links.html). The file
# has to exist even though it is not a page anyone reaches: just-the-docs
# builds the nav tree by matching each child's `parent:` to a real page title.
nav_link: false
# Never reachable, so keep it out of the search index and the sitemap.
search_exclude: true
sitemap: false
---
